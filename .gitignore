node_modules/
.next/
out/
build/
CLAUDE.md
.claude/
# env
.env
.env.local
.env.*.local
*.sql

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db

# editor
.vscode/
.idea/

# typescript
*.tsbuildinfo
next-env.d.ts

# Power BI report projects — reference only. The cost/group formulas are baked
# into src/ (api/finance/report + lib/finance/groups); the app never reads these
# at runtime. Patterns cover any future .pbip you drop in, whatever its name.
*.pbip
*.pbix
*.Report/
*.SemanticModel/

# Daily transaction exports — production data, never commit
Transaction_*.csv

# Generated report workbooks (scripts/gen-*.mjs) — production data
sd/
AE*_merchants_summary.xlsx

# Daily reconciliation exports (scripts/_run_rec_ultimate_to_csv.mjs) — production data
files/



